name: Documentation

# on:
#   schedule:
#     - cron: '0 0 */1 * *'
# TODO

on:
  push:
    branches:
      - add-actions

jobs:
  update-docs:
     name: update-docs
     runs-on: ubuntu-latest
     steps:
       - name: build
         uses: .github/action/build.yml
         with:
           token: ${{ secrets.GITHUB_TOKEN }}
